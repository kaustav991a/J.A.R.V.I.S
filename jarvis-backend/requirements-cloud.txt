# J.A.R.V.I.S. Cloud Gateway — minimal, headless-Linux-safe deps ONLY.
# Deliberately NOT the desk requirements.txt (no torch/tensorflow/pyaudio/pywin32).
# Keep this tiny so free-tier builds are fast and fit small memory limits.
aiogram==3.29.0
groq==1.2.0
fastapi==0.136.3
uvicorn[standard]==0.47.0
python-dotenv==1.2.2
ddgs==9.14.4
# C#11a Step 4: seals each PC-off turn to the desk's public half before queueing
# it. Sealing needs the public key only, so the cloud can never re-open its own
# queue. Vendored libsodium in a prebuilt wheel; pulls only cffi/pycparser.
pynacl==1.6.2
# Optional second brain, selected per capability by LLM_PROVIDER_* — see
# _provider_for in cloud_gateway.py. Import is guarded and every call falls back
# to Groq, so removing this line degrades the gateway rather than breaking it.
# 2.x is not optional. Pinned at 1.56.0 first, and every call came back
# `400 The legacy Interactions API schema is no longer supported. Please upgrade
# your google-genai Python SDK to version >= 2.0.0` — the server rejects the older
# wire format outright, so this is a floor rather than a preference.
google-genai==2.18.1
# Persistent rolling memory, used only when DATABASE_URL is set — see
# _memory_ready in cloud_gateway.py. Free Postgres (Neon, Supabase) is what exists;
# Render's own filesystem is ephemeral, so SQLite on the instance would die with the
# instance. The [binary] extra ships a prebuilt wheel, so no compiler is needed.
psycopg[binary]==3.3.2
